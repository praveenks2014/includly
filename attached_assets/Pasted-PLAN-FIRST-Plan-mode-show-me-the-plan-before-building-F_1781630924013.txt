PLAN FIRST — Plan mode, show me the plan before building. Finish the 2D
trial flow. The BACKEND FOUNDATION already exists — do NOT rebuild it:
- enum values trial_pending, trial_done already exist
- columns trial_fee_paid_inr, pre_meeting_requested, pre_meeting_note (on
matches) and trial_credit_inr, trial_credit_applied (on engagements)
already exist
- credit carry-over at commit (match.trialFeePaidInr →
engagement.trialCreditInr) already works
- salary deduction (first month minus credit, idempotent on retry)
already works
What's MISSING and needs building: the trial REQUEST → PAY → MARK-DONE →
COMMIT/NO-COMMIT flow and its UI.

FLOW TO BUILD (sequence: ₹500 matching fee is already upfront; then match
options → chat → price agreed → TRIAL → commit):

1. PARENT REQUESTS TRIAL (new): after price is informally agreed in chat,
parent clicks "Request Trial Day" on the candidate. New endpoint creates a
Razorpay order for the trial fee (a fixed admin_settings amount — confirm
this setting exists or add it). Optional checkbox "Request an intro
meeting before the trial" + a note → sets pre_meeting_requested /
pre_meeting_note. On successful payment verify (HMAC, same pattern as
existing flows): set trial_fee_paid_inr, transition match → trial_pending.

2. TEACHER SEES IT: the trial request + any pre-meeting note appears in
the teacher's Enquiries view for that candidate (reuse the existing
Enquiries UI — just surface the trial state + meeting note).

3. MARK TRIAL DONE (new): a way to transition trial_pending → trial_done
(decide and propose: who marks it — parent, teacher, or either? — and
where in the UI). This just records the trial happened; no payment.

4a. COMMIT after trial: from trial_done, parent commits (FREE — no charge,
like the existing commit). Engagement created; the existing credit
carry-over fires (trial fee → first-month credit). Confirm this reuses the
existing commit + carry-over code, doesn't duplicate it.

4b. NO-COMMIT after trial: parent walks away. Transition match →
cancelled. Per earlier decision: trial fee is KEPT by the teacher (NON-
refundable), record retained. Also RE-MASK the teacher's contact on
no-commit (it was unmasked during chat).

5. SAFEGUARDING: a trial can only be requested if the teacher is verified
(identity_verifications.status = verified) — block otherwise.

PAYMENT NOTE: build the trial-fee charge the SAME way as existing payments
(Razorpay order to the platform account) for now. I'm building Razorpay
Route (split) separately as Phase 4 — the trial fee will fold into that
split later (it's destined 100% to the teacher). Don't build a one-off
split here; just the standard order, so it's consistent and Route can
absorb it later.

CRITICAL — DON'T BREAK EXISTING FLOWS:
- The current flow (shortlist → free commit → engagement) must still work
for any match that DOESN'T go through a trial — trial should be an
OPTIONAL step, not forced on every match. Confirm a parent can still
commit directly without a trial if they choose.
- Do NOT modify the existing credit carry-over or salary-deduction code —
only call into them. Show me you're reusing, not rewriting.
- Do NOT touch the chat masking or the match scoring.

Show me the plan: the new endpoints, the exact state transitions
(shortlisted → trial_pending → trial_done → committed/cancelled), who
marks trial-done, the UI changes (parent side + teacher Enquiries side),
and confirm what existing code is REUSED vs new. NO code until I approve.