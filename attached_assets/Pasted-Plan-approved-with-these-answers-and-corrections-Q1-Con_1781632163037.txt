Plan approved with these answers and corrections:

Q1: Confirmed — matching fee 100% platform; salary + V2 sessions split.
PLUS: the TRIAL FEE also SPLITS (see Q2) — add it to the split table; it
was missing.

Q2 (corrected): The TRIAL FEE is split like the others — my commission %
is deducted to the platform account, the remainder settles to the
teacher's linked account. It is NOT 100% to the teacher. Use the same
commission rate as salary (salaryPlatformCutPct) unless I tell you
otherwise. So:
- Trial fee payment must be a Route split (commission to platform, rest
to teacher's linked account), and therefore requires the teacher's KYC to
be verified before the trial fee can be charged.
- IMPORTANT — trial-credit interaction: since commission was ALREADY taken
on the trial fee at trial time, make sure the trial-credit-toward-first-
month logic doesn't double-count or mis-apply commission. Show me exactly
how the trial fee split AND the first-month credit interact, so commission
is taken correctly once on each amount and the teacher's net is right
across (trial fee + first month). Don't guess this — lay out the math for
my review.

Q3: Use SOFT, not hard. Let the parent COMMIT and chat even if the
teacher's KYC isn't verified — only BLOCK the actual PAYMENTS (salary,
session, AND now trial fee) until the teacher is verified, with a clear
message. Don't block the commit/chat itself over pending paperwork.

Q4: Confirmed — ALL specialties (OT, speech, psychiatrists, shadow
teachers) use the same payout setup, not shadow-teacher-only.

Q5: Confirmed — Phase 1 refunds on split payments are admin-only via
Razorpay dashboard with a DB flag to surface them. Confirm the existing
matching-fee refund (100% platform, no split) stays UNCHANGED.

Two build corrections:
A. SENSITIVE DATA: don't store full PAN and bank account numbers unless
strictly necessary — Razorpay holds them once submitted. Store only the
linked-account id and MASKED values needed for re-display. Tell me the
minimum you actually need to store and why.
B. MIGRATION NUMBER: you named it 0015 but 0016/0017/0018 are already
applied. Renumber to the next free number (0019+) and confirm no
collision.

Still plan/review for money-touching parts: when you build, show me the
migration SQL and the changed payment-route code (the transfers array +
KYC guards, including the trial fee) BEFORE applying. Test mode only — no
live keys.